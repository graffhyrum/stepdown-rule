---
"@stepdown/analyzer": minor
---

Refactor CLI to use CommanderJS subcommands (`analyze` and `fix`) instead of flat structure with `--fix` flag. This provides better organization and follows Git-style CLI conventions.
