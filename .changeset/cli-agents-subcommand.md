---
"@stepdown/analyzer": minor
---

Add `agents` CLI subcommands (`analyze`, `fix`, `schema`) with a stable JSON envelope, semantic exit codes, and `--dry-run` fix preview. Harden human `analyze`/`fix` for automation (stderr diagnostics, exit codes, JSON fixes). Ship `SKILL.md` for agent workflows.
